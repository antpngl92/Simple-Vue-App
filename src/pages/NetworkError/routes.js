import NetworkError from '@/pages/NetworkError/NetworkError.vue'

const routes = [
  // pages
  {
    path: '/network-error',
    name: 'NetworkError',
    component: NetworkError
  }
]

export default routes
